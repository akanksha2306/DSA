package DataStructures.graphs.cycleDetection;

//this represents the vertices of the graph

/***
 * pseudo code
 * vertex.setVisited(false)
 * vertex.setBeingVisited(true)
 *
 * for(Vertex v : getAdjacencyList){
 *     if(v.isBeingVisited()){
 *      sout ("backward edge, so there is a cycle");
 *      return
 *}
 * if( !v.isVisited()){
 *      v.setVisited(true);
 *      dfs(v);
 *
 * }
 * }
 *
 *
 *
 *
 */
public class Vertex {
}
